package mylib
// import org.joda.time.DateTime

object TimeRange {

  /*
  def main(args: Array[String]): Unit = {
    val now = (new DateTime()).getMillis/1000
    println(s" #### now: ${}")
    def form(timestamp:Long) = {
      (new DateTime(1000*timestamp)).toString("yyyyMMddHH").toInt
    }
    def conv(hours:Int) = {
      form(now - hours*3600)
    }
    println("done")
  }
  */


}
